language: python
python:
  - "2.7"
# command to install dependencies
before_install:
  - "pip install -q cython"
  - "pip install -q numpy"
install:
  - "pip install -q ."
# command to run tests
script: nosetests
